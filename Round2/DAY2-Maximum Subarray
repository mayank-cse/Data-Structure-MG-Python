KADANE's ALGORITHM
****************************************
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        sumi = 0
        maxSum = max(nums)
        for i in range(len(nums)):
            sumi += nums[i]
            if sumi>maxSum:
                maxSum = sumi
            if sumi<0:
                sumi = 0
        return maxSum
